import IUser from "../types/IUser";
import axios from 'axios';

class AuthHtypService {
    private baseUrl = process.env.BASE_URL;

    public async findUser(credentials): Promise<void> {
        
    }
}